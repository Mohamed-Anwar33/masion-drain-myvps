// Import real content data from website migration
const { content: realContent } = require('./realContent');

// Use real content data from website migration
const contentSections = realContent;

module.exports = {
  contentSections
};